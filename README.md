A small project that solves room allocation problems in a soldier base, based on oop and using a basic server,
To activate, you must send a csv file to the server and it will parse and return results accordingly.
